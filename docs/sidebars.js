/**
 * Creating a sidebar enables you to:
 - create an ordered group of docs
 - render a sidebar for each doc of that group
 - provide next/previous navigation

 The sidebars can be generated from the filesystem, or explicitly defined here.

 Create as many sidebars as you want.
 */
const fg = require('fast-glob')

// @ts-check

const mainClass = ['Hoyolab', 'GenshinImpact', 'HonkaiImpact', 'HonkaiStarRail']

function replaceExtension(path) {
  return path.replace('./docs/', '').replace('.md', '')
}

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  guideSidebar: [
    {
      type: 'doc',
      label: 'Getting Started',
      id: 'guide/get-started',
    },
    {
      type: 'category',
      label: 'Client',
      items: [
        'guide/hoyolab',
        'guide/genshin-impact',
        'guide/honkai-impact',
        'guide/honkai-star-rail',
      ],
    },
    {
      type: 'doc',
      label: 'Error Handling',
      id: 'guide/error-handling',
    },
  ],
  apiSidebar: [
    {
      type: 'doc',
      id: 'api/index',
    },
    {
      type: 'category',
      label: 'Client Class',
      items: fg
        .sync(`./docs/api/**/(${mainClass.join('|')}).md`)
        .map(replaceExtension),
    },

    {
      type: 'category',
      label: 'Interface',
      items: fg.sync(`./docs/api/interfaces/*.md`).map(replaceExtension),
    },
    {
      type: 'category',
      label: 'Enum',
      items: fg.sync(`./docs/api/enums/*.md`).map(replaceExtension),
    },
  ],
  // By default, Docusaurus generates a sidebar from the docs folder structure
  // tutorialSidebar: [{type: 'autogenerated', dirName: '.'}],

  // But you can create a sidebar manually
  /*
  tutorialSidebar: [
    'intro',
    'hello',
    {
      type: 'category',
      label: 'Tutorial',
      items: ['tutorial-basics/create-a-document'],
    },
  ],
   */
}

module.exports = sidebars
